# Variables de entorno para el backend

# SEGURIDAD - Clave secreta para JWT (OBLIGATORIO en producción)
# Genera una clave segura con: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-generated-secret-key-here

# Base de datos (si usas PostgreSQL u otra BD)
# DATABASE_URL=postgresql://user:password@localhost/dbname

# Configuración de CORS
# ALLOWED_ORIGINS=http://localhost:3000,https://tudominio.com

# CONTROL DE REGISTRO DE USUARIOS
# Habilitar/deshabilitar el registro público de nuevos usuarios
# Útil para cerrar el registro después de crear los usuarios iniciales
REGISTRATION_ENABLED=true

# RATE LIMITING - Protección contra abuso
# Límites para el endpoint de registro
REGISTER_MAX_ATTEMPTS=5          # Número máximo de intentos de registro
REGISTER_WINDOW_MINUTES=60       # Ventana de tiempo en minutos (1 hora)

# Límites para el endpoint de login
LOGIN_MAX_ATTEMPTS=10            # Número máximo de intentos de login
LOGIN_WINDOW_MINUTES=15          # Ventana de tiempo en minutos
