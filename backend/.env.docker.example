# Variables de entorno para Docker Compose
# Este archivo es generado automáticamente por start.sh

# SEGURIDAD - Clave secreta para JWT (se genera automáticamente)
SECRET_KEY=your-secret-key-will-be-generated-automatically

# Base de datos SQLite (ubicación en el contenedor)
DATABASE_PATH=/app/data/control_gastos.db

# Variables adicionales (opcional)
# ALLOWED_ORIGINS=http://localhost:3000,https://tudominio.com
